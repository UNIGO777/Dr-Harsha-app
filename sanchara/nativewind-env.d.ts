/// <reference types="nativewind/types" />
// Enables the `className` prop on React Native components at the type level.

// Allow the global.css side-effect import (compiled by Metro via NativeWind).
declare module '*.css';
