# features/auth
Phone-OTP auth logic: request/verify OTP mutations (React Query hooks over
`api` + `endpoints.auth`), session establishment via `authStore`. UI lives in
`app/(auth)/`; reusable pieces and hooks live here.
