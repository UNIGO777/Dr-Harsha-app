# features/onboarding
13-field onboarding logic: per-step schemas (mirror backend), the age 30–60
eligibility gate + waitlist branch (`lib/validators`), and the final submit
mutation. Draft state lives in `store/onboardingStore`. UI in `app/(onboarding)/`.
