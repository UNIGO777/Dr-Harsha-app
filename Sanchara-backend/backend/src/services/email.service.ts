/**
 * Email service — nodemailer.
 *
 * TODO (next session): create a nodemailer transport from SMTP_* env vars.
 * TODO: when SMTP_* is absent (local phase), fall back to a mock/preview
 *       transport (e.g. log the email or use nodemailer's stream transport)
 *       so the app boots and "sends" without real credentials.
 * TODO: expose `sendEmail({ to, subject, html, text })` and typed template
 *       helpers (OTP, welcome, appointment reminders).
 */

export {};
