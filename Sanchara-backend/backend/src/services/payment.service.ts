/**
 * Payment service — Razorpay.
 *
 * TODO (next session): construct a Razorpay client from RAZORPAY_KEY_ID /
 *       RAZORPAY_KEY_SECRET; expose order creation + webhook signature
 *       verification.
 * TODO: in local phase, MOCK live payments — generate fake order ids and treat
 *       payments as successful so no live keys are needed to boot/test.
 * NOTE: never trust client-reported payment status; always verify signatures.
 */

export {};
