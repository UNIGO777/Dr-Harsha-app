/**
 * LLM service — @anthropic-ai/sdk (Claude).
 *
 * TODO (next session): construct an Anthropic client from ANTHROPIC_API_KEY and
 *       expose a chat helper for the patient chatbot.
 * TODO: enforce CHATBOT_DAILY_LIMIT per user before calling the API.
 * TODO: when ANTHROPIC_API_KEY is absent (local phase), MOCK — return a canned
 *       response so chatbot flows work without a real key or spend.
 * TODO: default to the latest capable Claude model; keep the model id in config.
 */

export {};
