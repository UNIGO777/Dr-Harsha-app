/**
 * Push notification service — firebase-admin (FCM).
 *
 * TODO (next session): initialize firebase-admin from the service-account JSON
 *       referenced by FIREBASE_SERVICE_ACCOUNT.
 * TODO: when FIREBASE_SERVICE_ACCOUNT is absent (local phase), MOCK — log the
 *       notification payload and no-op so the app boots without Firebase creds.
 * TODO: expose `sendPush({ token(s), title, body, data })`.
 */

export {};
